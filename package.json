{
  "name": "ppt-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "16:9 slide deck viewer with keyboard/click navigation and theming",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.4",
    "jsdom": "26.1.0",
    "vitest": "3.2.4"
  }
}
